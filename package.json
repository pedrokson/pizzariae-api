{
  "name": "pizzaria-api",
  "version": "1.0.0",
  "main": "backend/app.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm start",
    "install": "cd backend && npm install"
  },
  "keywords": ["pizzaria", "backend", "api", "node", "express", "mongodb"],
  "author": "Pedro", 
  "license": "ISC",
  "description": "Backend API da Pizzaria - Sistema híbrido MongoDB + Memória",
  "dependencies": {}
}
